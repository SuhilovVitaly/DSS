# Ревью (Phase 4): Мигающий индикатор и угловая линия подписи космического объекта

- Iteration ID: 20260806T185608Z
- Review ID: 20260806T191039Z
- Основное ТЗ: `plan-20260806T185818Z.md`
- Учтенные ТЗ: `plan-20260806T185818Z.md`
- Учтенные ревью: нет
- Дата (UTC): 2026-08-06T19:10:39.1263378Z
- Ревьюер: ChatGPT/Codex (Phase 4)

## 1. Вердикт

ACCEPTED WITH NOTES - функциональные критерии приемки выполнены, build/tests проходят; найден только низкий риск устаревшего комментария в коде.

## 2. Проверяемое утверждение

Status square имеет размер 8x8 px и в `Speed1` рисуется в интервале `[0, 1000) ms` каждого 2000 ms цикла, не рисуется в `[1000, 2000) ms`, затем повторяет цикл. В `Speed0` status square не мигает и остается видимым исходным object color. Leader line endpoint равен ближайшему углу `PlaqueRect` к object screen point.

## 3. Толерансы

Геометрические сравнения допускают обычную float precision, зафиксированную unit-тестами через `SKPoint`/`SKRect`. Границы фаз мигания должны быть точными: `0` и `999` ms видимы, `1000` и `1999` ms невидимы, `2000` ms снова видим.

## 4. Профиль эксплуатации

Проверяемая область ограничена client-side tactical map rendering: разные screen positions объектов, viewport clamp панели, все четыре угла label-плашки, paused/unpaused simulation speed. Engine, Contracts, Motion, snapshot DTO, content JSON, сценарии, requirements и tactical palette не должны изменяться.

## 5. Портфель доказательств

- Build: `dotnet build DeepSpaceSaga.sln` - passed, 0 warnings, 0 errors.
- Tests: `dotnet test tests\DeepSpaceSaga.Client.Tests\DeepSpaceSaga.Client.Tests.csproj --filter FullyQualifiedName~ObjectLabelTests` - passed, 40/40.
- Tests: `dotnet test DeepSpaceSaga.sln` - passed, Contracts 6/6, Motion 8/8, Engine 83/83, Client 266/266.
- Ручное ревью: проверены `ObjectLabelLayout`, `ObjectLabelRenderer`, `StatusSquareAnimator`, `ObjectLabelTests`, `git diff --stat`, `git diff`, `git status --short`.
- Требования DSS: сверены `AGENTS.md`, `CLAUDE.md`, `deep_space_saga_engine_requirements.md` разделы 39 и 42, `Docs/TacticalMapColors.md`.
- Архитектурные границы: изменения находятся только в `DeepSpaceSaga.Client` и `DeepSpaceSaga.Client.Tests`; `Engine`, `Contracts`, `Motion`, snapshot/session contracts и palette не изменены.
- Прочие техники: dependency boundary дополнительно проверена чтением `.csproj` и поиском `SkiaSharp`/`Silk`/`System.Drawing`; graphics-зависимости остаются в Client.

## 6. Соответствие ТЗ

- Добавить: ok - добавлены/обновлены тесты видимости status square и выбора всех четырех углов.
- Изменить: ok - helper заменен на binary visibility, renderer пропускает `DrawRect` в hidden phase, leader endpoint выбирается из углов после viewport clamp.
- Удалить: ok - smooth color blending и `LeaderEdgeMargin` удалены из актуального поведения и тестовых ожиданий.
- Защитить: ok - Engine/Contracts/Motion/requirements/palette/content/scenario не затронуты.
- Единицы работы: ok - 4.1, 4.2 и 4.3 выполнены по diff и тестам.
- Критерии приемки: ok - подтверждены unit-тестами, build/full test и ручным ревью renderer/layout.

## 7. Находки

### 7.1. low - Устаревший комментарий про bottom edge

- Тип: defect
- Доказательство: `src/DeepSpaceSaga.Client/UI/Screens/GameSession/ObjectLabelLayout.cs:83` содержит комментарий `Leader line endpoint — nearest point on bottom edge`, но строки 105-132 фактически выбирают ближайший угол панели.
- Почему важно: комментарий может сбить следующего исполнителя и вернуть старую edge-based модель при будущей правке layout.
- Рекомендация: в следующем Phase 3 или мелком cleanup заменить комментарий на формулировку про nearest plaque corner.
- Маршрутизация: Phase 3

## 8. Выбрано для следующего цикла

Не подтверждено оператором. По умолчанию в следующий цикл можно передать только пункт 7.1 как низкоприоритетный cleanup; функционального возврата в Phase 3 не требуется.

## 9. Регрессионная защита

Существующие `ObjectLabelTests` защищают:

- видимость status square на `0`, `999`, `1000`, `1999`, `2000` ms;
- стабильную видимость status square при `Speed0`;
- период 2000 ms;
- размер status square 8x8 px;
- выбор каждого из четырех углов через `GetLeaderEndPoint`;
- то, что `Create(...)` возвращает endpoint, совпадающий с одним из углов `PlaqueRect`.

Дополнительно можно добавить легковесный renderer-level тест или seam только если появится тестовая инфраструктура для проверки отсутствия `DrawRect`; текущий план явно разрешал ограничиться helper unit-тестами без сложной Skia-инструментации.

## 10. Остаточный риск

- Визуальный smoke-test реального окна не запускался: валидация опирается на unit-тесты и ручное ревью Skia draw branch.
- Off-фаза `ObjectLabelRenderer` подтверждена ручным ревью, а не инструментированным canvas/mock test.
- В workspace есть untracked process artifacts `.claude/`, `Agents/`, `Stories/`; они не являются кодом Phase 3 и не меняют runtime behavior.
- Вспомогательная команда `dotnet list package` не использовалась как доказательство из-за sandbox-доступа к пользовательскому `NuGet.Config`; архитектурная граница проверена чтением project files и поиском references.
